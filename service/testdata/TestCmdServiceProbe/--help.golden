Probe a service for proper TLS setup & configuration

Usage:
  anchor service probe <service> [flags]

Flags:
  -h, --help               help for probe
  -o, --org string         Organization of the service to probe.
  -r, --realm string       Realm instance of the service to probe.
  -s, --service string     Service to probe.
      --timeout duration   Time to wait for a successful probe of the service. (default 2m0s)

Global Flags:
      --api-token string   Anchor API personal access token (PAT).
      --config string      Service configuration file. (default "anchor.toml")
      --skip-config        Skip loading configuration file.
